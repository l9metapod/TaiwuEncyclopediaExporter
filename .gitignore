# === 编译产物 ===
bin/
obj/
*.dll.config
*.pdb
*.user
*.suo
.vs/

# === 导出产物（由 Mod 运行时生成，不入库）===
Mod/EncyclopediaExporter/output/

# === Python ===
__pycache__/
*.pyc

# === 系统/编辑器 ===
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.idea/

# === 游戏目录联接（junction）===
# Game 是指向本机 Steam 游戏目录的符号链接，仅供本地开发查看资源/反编译代码，
# 绝不可提交（会卷入整个游戏本体，且含版权内容）。
/Game

# === 本地迁移文档（含本地路径，不提交）===
/HANDOFF.md

# === 卡片探针产物（开发调试用，不入库）===
# raw/ 是原始 Player.log 备份；samples/ 是去重 dump 样本（游戏更新后可能要重 dump）。
# colors_table.txt 作为例外保留（188 条颜色表，有长期参考价值）。
/_probe/raw/
/_probe/samples/

# === 杂项：Windows 设备名误生成的垃圾文件 ===
/nul
